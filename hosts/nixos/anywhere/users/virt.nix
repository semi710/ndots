# Empty — home-manager config goes here when you add a `users/` entry.
# nix-wire auto-discovers users/<name>.nix and wires it as home-manager
# config for that user.  See hosts/nixos/obox/users/nikhil.nix for example.
{ ... }: { }
